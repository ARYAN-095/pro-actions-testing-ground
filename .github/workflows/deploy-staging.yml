name: Deploy to Staging

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a placeholder. The real staging deployment runs from the 'develop' branch."
